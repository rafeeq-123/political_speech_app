  <%= form_for :speech do |f|%>

  <%= f.fields_for :comments do |comment| %>
  <%= comment.label :name%>
  <%= comment.text_field :name%>
  <%= comment.label :comment%>
  <%= comment.text_field :comment%>
  <%= comment.submit "Leave a Comment"%>
 <%end%>
<%end%>

